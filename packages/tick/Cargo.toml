[package]
name = "belugaswap-tick"
version = "0.1.0"
edition = "2021"

[dependencies]
soroban-sdk = { workspace = true }
belugaswap-math = { path = "../math" }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }

[lib]
crate-type = ["cdylib", "rlib"]
